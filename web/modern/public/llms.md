# One API LLM Instructions

Canonical LLM instructions are available at `/llms.txt`.

Use `/openapi.json` for API schemas and include `Authorization: Bearer <relay-api-key>` on `/v1/*` and `/mcp` requests.
